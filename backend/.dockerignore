# Minimal safe .dockerignore for Laravel
.git
.gitignore
README.md
Dockerfile
.dockerignore
.env
.env.backup
.env.production

# IDE files
.vscode
.idea
.phpactor.json

# OS files
.DS_Store
Thumbs.db

# Node modules
node_modules

# Don't ignore these critical Laravel files:
# - artisan (needed for Laravel commands)
# - public/ (needed for web server)
# - bootstrap/ (needed for Laravel bootstrap)
# - storage/ (needed but we'll create structure)
